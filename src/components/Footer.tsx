import { LanguageSelector } from "./LanguageSelector";

export const Footer = () => {
  return (
    <footer className="max-w-5xl mx-auto px-4 md:px-0 py-8">
      <LanguageSelector />
    </footer>
  );
};
