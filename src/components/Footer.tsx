import { LanguageSelector } from "./LanguageSelector";

export const Footer = () => {
  return (
    <footer className="w-full max-w-5xl mx-auto px-4 py-8">
      <LanguageSelector />
    </footer>
  );
};
